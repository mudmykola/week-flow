<script setup lang="ts">
const { data: activities, status } = await useFetch('/api/activity')
</script>
<template><div class="mx-auto max-w-4xl px-5 py-8 md:px-10"><h1 class="font-display mb-8 flex items-center gap-3 text-3xl"><UIcon name="i-lucide-activity" class="text-[var(--color-accent)]"/>Активність</h1><div v-if="status==='pending'" class="space-y-3"><USkeleton v-for="i in 4" :key="i" class="h-16 rounded-xl"/></div><div v-else class="glass-panel divide-y divide-[var(--color-panel-border)]"><div v-for="activity in activities" :key="activity.id" class="flex items-center gap-4 p-4"><UIcon name="i-lucide-history" class="size-4 text-secondary"/><div class="flex-1"><p class="text-sm">{{ activity.action }}</p><p class="text-xs text-secondary">{{ activity.entityType }} · {{ activity.entityId }}</p></div><time class="text-xs text-secondary">{{ new Date(activity.createdAt).toLocaleString('uk-UA') }}</time></div><p v-if="!activities?.length" class="p-10 text-center text-secondary">Активності ще немає</p></div></div></template>
